{
  "name": "root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd warehouse && npm run dev\" \"cd client && npm run dev\"",
    "install-all": "concurrently \"cd warehouse && npm install\" \"cd client && npm install\" && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 